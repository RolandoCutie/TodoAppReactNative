{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attatch to packager3",
            "type": "reactnative",
            "request": "attach",
            "port": 8081,
            "cwd": "${workspaceFolder}"
        }
    ]
}
